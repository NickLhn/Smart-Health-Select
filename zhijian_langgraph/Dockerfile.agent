# AI Agent 服务 Dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 18081

CMD ["python", "-m", "uvicorn", "agent.app:app", "--host", "0.0.0.0", "--port", "18081"]
